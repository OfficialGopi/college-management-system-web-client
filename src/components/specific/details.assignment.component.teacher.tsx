import { useParams } from "react-router-dom";

const DetailsInAssignmentsComponentTeacher = () => {
  const { assignment } = useParams();

  return <div className="w-full"></div>;
};

export default DetailsInAssignmentsComponentTeacher;
